function countOfAllIndexMatchingNumbers(nums) {
  // Insert code here;
  //Initialize a count to keep track of all numbers matching the index
  let count = 0;
  //Use a for loop to iterate through the array and check if each number match its index
  for(let i = 0; i < nums.length; i ++){
    if(nums[i] == i{
      count++
    }
  }
  count;
}

// Do not edit this line;
module.exports = countOfAllIndexMatchingNumbers;
